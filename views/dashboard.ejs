<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title><%= user.email %>'s Dashboard | The Bookmark</title>
    <style>
        h2 {
            font-size: clamp(0.75rem, 2.5vw, 1.8rem);
            word-break: break-word;
            max-width: 100%;
        }
        .dropdown {
            position: relative;
            display: inline-block;
            margin-right: 1rem;
        }

        .dropdown-btn {
            background-color: #3498db;
            color: white;
            padding: 10px;
            font-size: 16px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }

        .dropdown-content a {
            color: black;
            padding: 10px 12px;
            text-decoration: none;
            display: block;
        }

        .dropdown-content a:hover {
            background-color: #f1f1f1
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Responsive dropdown */
        @media (max-width: 600px) {
            .top h1 {
                font-size: 14px;
            }
            .dropdown-btn {
                font-size: 14px;
                padding: 8px;
            }
        }
    </style>
</head>
<body>
    <%- include("partials/nav.ejs") %>
    <div class="container1">
        <div class="modal1">
            <div class="top">
                <div class="dropdown">
                    <button class="dropdown-btn">Manage account</button>
                    <div class="dropdown-content">
                        <a href="/add-book">Add Book</a>
                        <a href="/logout">LogOut</a>
                        <a href="/completed">Completed Books</a>
                        <a href="/reading">Ongoing Books</a>
                        <a href="/dashboard">All Books</a>
                    </div>
                </div>
                <h2>Welcome <%= user.email %></h2>
            </div>


            <div class="book-list">
                <% if (books.length > 0) { %>
                    <% books.forEach(book => { %>
                        <div class="book-item">
                            <a href="/books/<%= book.id %>">
                                <h2><%= book.title %></h2>
                                <p>Genre: <%= book.genre %></p>
                                <p>Total Pages: <%= book.pages %></p>
                            </a>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p>No books found.</p>
                <% } %>
            </div>
        </div>
    </div>
    <%- include("partials/footer.ejs") %>
</body>
</html>
